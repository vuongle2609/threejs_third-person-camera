# THREEjs third person camera demo

camera suck

Change these properties at calculateIdealOffset and calculateIdealLookat in camera.ts for better camera control :(

```javascript
const maxY = number;
const minY = number;
```
